# Set the URL of the web application to interact with
ui.url=https://dev.insurance.tekschool-students.com/
# Specify the web browser to be used for automation or testing
ui.browser=chrome
# Configure the browser to run in headless mode (without a graphical user interface)
ui.browser.headless=true